apiVersion: v1
kind: Service
metadata:
  name: deploy-to-aks
  namespace: dev
  labels:
    app.kubernetes.io/name: deploy-to-aks
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: deploy-to-aks
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000